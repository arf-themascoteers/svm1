## What this is
A tiny, expandable sandbox to build SVM intuition from first principles with small Python experiments.

## Setup
python -m pip install -r requirements.txt

## Run
python svm_platform.py
